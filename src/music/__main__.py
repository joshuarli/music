"""music — audio library CLI tools.

Usage:
    uv run scan ...       # probe + analyse audio files
    uv run tag ...        # fingerprint + look up metadata + write tags
    uv run transcode ...  # transcode audio to AAC/M4A
"""
